# Deploy do site oficial — Next.js (static export) em site-next/
# Publicacao real e feita via Netlify CLI (skill /publicar-site); auto-deploy via Git esta desligado.
[build]
  base = "site-next"
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
